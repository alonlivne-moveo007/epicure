$kind: http-request
name: GET /api/tags/:id
description: Get a single tag by document id. `populate=*` includes first-level
  relations and media.
url: "{{baseUrl}}/api/tags/{{tagId}}?populate=*"
method: GET
queryParams:
  populate: "*"
order: 2000
