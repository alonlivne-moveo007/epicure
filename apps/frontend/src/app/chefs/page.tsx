/**
 * Root chefs route (`/chefs`): placeholder until chefs listing is implemented.
 */
import { SectionWrapper } from '@/components/layout/SectionWrapper/SectionWrapper';

export default function ChefsPage() {
  return (
    <SectionWrapper title="Chefs" titleId="chefs-heading">
      {null}
    </SectionWrapper>
  );
}
